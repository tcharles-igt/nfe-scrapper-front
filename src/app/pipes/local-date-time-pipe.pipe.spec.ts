import { LocalDateTimePipePipe } from './local-date-time-pipe.pipe';

describe('LocalDateTimePipePipe', () => {
  it('create an instance', () => {
    const pipe = new LocalDateTimePipePipe();
    expect(pipe).toBeTruthy();
  });
});
